






//  get available structure and camera in django data base use get_str_camera view 

// so  this is the structure modele and the camera 
// class Structure(models.Model):
//       StructureID = models.AutoField(primary_key=True)
//       StructureLable = models.CharField(max_length=50, null=True, blank=True)

// class Camera(models.Model):
//       CameraID = models.AutoField(primary_key=True) 
//       CameraLable = models.CharField(max_length=50, null=True, blank=True)
//       StructureID = models.OneToOneField(Structure, on_delete=models.CASCADE)
//       IsActive = models.BooleanField(default=False)

 
//  use those structures and camera to create  tow select input  for select  the structure and the camera depend on this structure 


// so give me the react component for display the input for struccture and camera 
/// also give me the get_str_camera view

//  